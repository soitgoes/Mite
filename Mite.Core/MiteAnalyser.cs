namespace Mite.Core
{
    public class MiteAnalyser
    {
        public MiteAnalyser()
        {
            
        }

        public string[] Analyze()
        {
            //migrate a temp database up from scratch export the results

            //export the results of the present schema minus the database and diff the results
            return null;
        }
    }
}